import React from 'react';
import { Typography, Container } from '@mui/material';

export default function Home() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Board App
      </Typography>
      <Typography>Frontend skeleton — implement UI here.</Typography>
    </Container>
  );
}
